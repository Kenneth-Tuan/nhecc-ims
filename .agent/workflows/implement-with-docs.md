---
description:
---

Context Check: Quickly scan llms.txt to identify required Nuxt UI components.

Architecture Plan: Propose a file structure (e.g., new composables/ if logic is reusable).

Implementation:

Build with Nuxt 4 patterns.

Implement Zod validation.

Check: Ensure NO watch is used unless absolutely necessary.

Commit: Provide a Conventional Commit message.
